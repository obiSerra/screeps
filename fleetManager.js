function handleFleet(roomName) {
  const room = Game.rooms[roomName];
  
  const baseFleet = {
    harvesters: 2,
    builders: 1,
    upgraders: 1,
  };
}

module.exports = () => {
  let status = {
    fleet: {},
  };
};
